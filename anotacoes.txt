Instalando gulp (dependência local do desenvolvedor):
npm install gulp@3.9.0 --save-dev

Intalando imagemin:
npm install gulp-imagemin@2.3.0

No arquivo package.json, inserir uma nova linha dentro de script para criar um alias para o gulp:
"scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gulp": "gulp"
  },